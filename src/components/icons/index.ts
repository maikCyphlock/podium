import * as LucideIcons from 'lucide-react';

export const Icons = {
  ...LucideIcons,
  // You can also add custom icons here in the future
};

// Still keep the direct exports if needed elsewhere
export * from 'lucide-react';